<script lang="ts">
	import { CalendarPlus, Upload } from '@lucide/svelte';
	import Faq from '../Faq.svelte';
	import ManualSection from '../ManualSection.svelte';
	import UiLabel from '../UiLabel.svelte';
	import { SCREEN } from '../labels';
	import type { Edition } from '../edition';

	let { edition }: { edition: Edition } = $props();
</script>

<ManualSection id="trouble">
	<Faq q="「よみこめませんでした」と出る">
		{#if edition === 'lite'}
			<p>
				画面をいったん閉じて、ひらき直してください。それでも直らないときは、端末を再起動してみてください。
			</p>
			<p>
				プライベートブラウズ（シークレットモード）でひらいていないかも確かめてください。その場合は赤い帯もいっしょに出ます。
			</p>
		{:else}
			<p>
				おうちのサーバーが動いているか確かめてください。よくあるのは、サーバーを動かしているパソコンやミニPCの電源が落ちている、Wi-Fi
				がつながっていない、というものです。
			</p>
			<p>ほかの端末からも同じように出るなら、サーバー側の問題です。</p>
		{/if}
	</Faq>

	<Faq q="チェックしたのに点数が上がらない">
		<p>次のどれかにあてはまることが多いです。</p>
		<ul>
			<li>
				<strong>「{SCREEN.checkNotDone}」を押している</strong> —
				点は入りません。「{SCREEN.checkDone}」か「{SCREEN.checkCancelled}」なら入ります
			</li>
			<li>
				<strong>生活と宿題のどちらかが空になっている</strong> —
				片方が0件だとその区分は0点なので、もう片方を全部やっても最大50点にしかなりません。<strong
					>両方とも空なら、ずっと 0点 のままです</strong
				>（「からっぽ」で始めたときにこうなります。この場合は注意も出ません）。設定の「せいかつ」「しゅくだい」タブに、それぞれ1つ以上入れてください
			</li>
			<li>
				<strong>チャレンジがまだ開いていない</strong> —
				基本点が100点になるまでは、チャレンジをつけても点は足されません
			</li>
		</ul>
	</Faq>

	<Faq q="読み上げのボタンが出てこない">
		{#if edition === 'lite'}
			<p>
				<strong>この版に読み上げの機能はありません。</strong
				>音声はサーバー側で作るしくみなので、ブラウザだけで動くこの版では出ません。
			</p>
			<p>読み上げを使いたい場合は docker 版が必要です。</p>
		{:else}
			<p>
				VOICEVOX が動いていないと出ません。VOICEVOX を起動してから、画面をひらき直してください。
			</p>
			<p>設定画面の「きほん」タブでも、使える状態かどうかを確かめられます。</p>
		{/if}
	</Faq>

	<Faq q="子どもが設定をいじってしまう">
		{#if edition === 'lite'}
			<p>
				<strong>この版に PIN のような鍵はありません。</strong
				>画面だけで動くしくみなので、隠しても意味がないためです。
			</p>
			<p>
				端末側の機能で制限してください。iPhone・iPad なら「スクリーンタイム」、Android なら「ファミリーリンク」やアプリのロック機能が使えます。
			</p>
		{:else}
			<p>
				<strong>PIN をかけられます。</strong>設定すると、親用の画面をひらくときに数字を聞かれるようになります。
			</p>
			<p>設定のしかたは、このアプリを入れてくれた方に頼んでください（起動時の設定項目です）。</p>
		{/if}
	</Faq>

	<Faq q="来年もつづけて使いたい">
		<p>
			設定の一覧で <UiLabel><CalendarPlus size={13} />来年ぶんをつくる</UiLabel> を押してください。
		</p>
		<p>
			学年が1つ上がり、日づけが1年後にずれた設定ができます。項目はそのまま引き継がれるので、日づけだけ実際の夏休みに合わせて直せば使えます。<strong
				>今年のチェック記録は引き継ぎません</strong
			>（新しい年は、新しい記録で始まります）。
		</p>
		<p>
			お子さんの画面に出る年は、<strong>今日の日づけから自動で決まります</strong
			>。画面から年を選ぶことはできません。
		</p>
		<ul>
			<li>夏休みの<strong>期間中</strong>なら、その年のぶん</li>
			<li>どの期間にも入っていないときは、<strong>終わった年のうち、いちばん新しいぶん</strong></li>
			<li>
				終わった年がまだ1つも無いとき（登録したてで、夏休みがこれからのとき）は、<strong
					>いちばん近い、これからの年のぶん</strong
				>
			</li>
		</ul>
	</Faq>

	<Faq q="記録が消えてしまった">
		{#if edition === 'lite'}
			<p>
				設定画面の <UiLabel><Upload size={13} />もどす</UiLabel> から、とっておいたバックアップのファイルを選んでください。
			</p>
			<p>
				バックアップが無い場合、残念ながら元にはもどせません。記録はその端末の中にしか無いためです。これからは、ときどきバックアップをとっておいてください。
			</p>
		{:else}
			<p>
				サーバーの <code class="rounded bg-surface2 px-1">data</code> フォルダを、前にコピーしておいたものに書きもどしてください。
			</p>
			<p>操作は、このアプリを入れてくれた方に頼んでください。</p>
		{/if}
	</Faq>

	<Faq q="まちがえて設定を消してしまった">
		<p>
			<strong>設定を消しても、チェック記録は消えていません。</strong
			>消す前に「エクスポート（JSON）」で書き出したファイルがあれば、「JSON をインポート」で取り込み直すことで、残っている記録とまた結びつきます。
		</p>
	</Faq>

	<Faq q="夏休みが終わったあとも見られますか">
		<p>
			見られます。期間が過ぎるとその日のチェック欄は出なくなりますが、これまでの記録の表・ごほうび・新学期の準備はそのまま使えます。記録の表から前の日を直すこともできます。
		</p>
	</Faq>
</ManualSection>
